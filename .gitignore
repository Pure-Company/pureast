# Binaries
*.exe
*.dll
*.so
*.dylib
/pureast
/cmd/pureast/pureast

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Output
/output/
*.extracted.go
/pureast-mcp
/.pureast-index.json
/my-index.json

# DOT graphs from `pureast deps --format dot > graph.dot`
*.dot

# Files accidentally created when a flag eats its argument
# (e.g. `pureast deps X . --format json -o`  → `-o` becomes a stray empty file)
/-o
/-f
